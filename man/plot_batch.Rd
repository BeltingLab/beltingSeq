% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_batch}
\alias{plot_batch}
\title{Create batch effect visualization with boxplot and PCA}
\usage{
plot_batch(.matrix, .coldata, .condition, .batch = NULL, title)
}
\arguments{
\item{.matrix}{Data frame with gene expression data. First column should be SYMBOL,
remaining columns are samples (column names must match .coldata$sample)}

\item{.coldata}{Data frame with sample metadata. Must contain a "sample" column with
values matching the column names of .matrix (excluding SYMBOL column)}

\item{.condition}{Character string specifying the column name in .coldata for the
condition of interest (used for coloring and grouping)}

\item{.batch}{Character string specifying the column name in .coldata for the
batch or confounding variable (used for PCA shapes). Optional if .coldata has a "labels" column}

\item{title}{Character string for plot titles}
}
\value{
Combined ggplot object with boxplot and PCA arranged vertically
}
\description{
Generates a two-panel plot for quality control and batch effect visualization.
The top panel shows a boxplot of sample intensities grouped by condition, and the bottom
panel shows a PCA plot colored by condition and shaped by batch variable.
}
\examples{
\dontrun{
# Create sample data
expr_matrix <- data.frame(
  SYMBOL = paste0("Gene", 1:100),
  Sample1 = rnorm(100, 10, 2),
  Sample2 = rnorm(100, 10, 2),
  Sample3 = rnorm(100, 12, 2),
  Sample4 = rnorm(100, 12, 2)
)
coldata <- data.frame(
  sample = c("Sample1", "Sample2", "Sample3", "Sample4"),
  condition = c("Control", "Control", "Treatment", "Treatment"),
  batch = c("Batch1", "Batch2", "Batch1", "Batch2"),
  labels = c("C_B1", "C_B2", "T_B1", "T_B2")
)
plot <- plot_batch(expr_matrix, coldata, "condition", "batch", "Normalized Data")
}
}
